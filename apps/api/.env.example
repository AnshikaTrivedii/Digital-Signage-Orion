DATABASE_URL="postgresql://postgres:postgres@localhost:5432/orion_platform?schema=public"
# Optional locally (defaults to DATABASE_URL). Required on Supabase production:
# set to the *direct* connection (db.<ref>.supabase.co), not the pooler URL.
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/orion_platform?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"
PORT=3001

# Public URL for player download links when using local disk storage (Render sets RENDER_EXTERNAL_URL automatically)
API_PUBLIC_URL=http://localhost:3001

# Fallback full-manifest poll interval in seconds (safety net only).
# Playlist changes are delivered on heartbeat FORCE_SYNC. Defaults to 600s (10 min); clamped to 30–3600s.
PLAYER_SYNC_INTERVAL_SECONDS=600

# Lightweight /player/sync-revision poll interval. 0 = disabled (default).
# Content changes arrive via heartbeat FORCE_SYNC / syncRequired. Clamped to 0–3600s.
PLAYER_REVISION_POLL_INTERVAL_SECONDS=0

# S3 Storage (auto-uses local disk when S3_ACCESS_KEY_ID is still the placeholder)
S3_USE_LOCAL_STORAGE=true
S3_BUCKET=orion-assets
S3_REGION=ap-south-1
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key
S3_ENDPOINT=                        # optional, for MinIO/DO Spaces
S3_FORCE_PATH_STYLE=false           # set true for MinIO
