/**
 * Temporary product flags. Set to true to re-enable hidden features.
 * Layout Designer code and API routes remain in the repo — only CMS access is gated.
 */
export const LAYOUT_DESIGNER_ENABLED = false;
